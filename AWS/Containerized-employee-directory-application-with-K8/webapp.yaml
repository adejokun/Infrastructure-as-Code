apiVersion: v1
kind: Namespace-2
metadata:
  name: employee-dir-app

---

apiVersion: v1
kind: Service
metadata:
  namespace: employee-dir
  name: employee-dir-service-2
spec:
  type: NodePort
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      
---

apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: employee-dir
  name: employee-dir-depl-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: employee-dir-2
        image: 992382381749.dkr.ecr.us-west-2.amazonaws.com/employee-directory
        imagePullPolicy: Always
        ports:
        - containerPort: 80

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: employee-dir
  name: ingress-employee-dir-2
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: employee-dir-service-2
              port:
                number: 80
